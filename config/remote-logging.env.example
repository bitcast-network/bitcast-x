# Optional remote logging. It is disabled when BITCAST_X_LOKI_URL is empty.
# Use an operator-owned endpoint and a write-only token; this file must never
# contain a real credential in source control.

BITCAST_X_LOKI_URL=
# BITCAST_X_LOKI_USERNAME=
# BITCAST_X_LOKI_TOKEN=
